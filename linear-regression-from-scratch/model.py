class LinearRegression:
    def __init__(self):
        self.weight = 0
        self.bias = 0
        
        # Now add a prediction method.
    def predict(self, x):
        return self.weight * x + self.bias
       # That's it.

    # One line.

    # But that's

  # y = Wx + b

  # in code.